function ChatbotWidget() {
  // CoreLinq chatbot widget — not yet configured.
  // When ready to enable, add "use client" directive, useEffect with script injection,
  // and return a <div id="corelinq-chatbot" /> mount point.
  return null;
}

export default ChatbotWidget;
